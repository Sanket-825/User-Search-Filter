# User-Search-Filter
Dynamic User Finder is a responsive search filter web app built with HTML, CSS (Bootstrap), and JavaScript. It displays user cards with names, bios, and images, and allows real-time searching with debounced input handling for smoother performance. Includes a “No user found” state and dynamic card generation using JavaScript DOM manipulation.
